import React from 'react'
import {Button} from '@patternfly/react-core/dist/js/'

const ReactCoreCJS = () => {
  <div>
    <Button>I am just a button</Button>
  </div>
}

export default ReactCoreCJS;
